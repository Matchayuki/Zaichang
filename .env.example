# Copy to .env locally; never commit the actual key.
AI_API_KEY=
AI_BASE_URL=https://api.deepseek.com
AI_MODEL=deepseek-v4-pro
HOST=127.0.0.1
PORT=4182
# Exact public frontend origin, only needed when deployed separately.
APP_ORIGIN=
